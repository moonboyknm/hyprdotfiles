/* Waybar Neon Red Theme */

* {
  border: none;
  border-radius: 0;
  font-family: "JetBrainsMono Nerd Font", monospace;
  font-size: 15px;
  min-height: 0;
}

window#waybar {
  background-color: rgba(0, 0, 0, 0.9);
  border: 2px solid #ff0040;
  border-radius: 0;
  color: #ffffff;
  transition-property: background-color;
  transition-duration: 0.5s;
}

window#waybar.hidden {
  opacity: 0.2;
}

#workspaces {
background: #000000;
margin: 2px 5px;
padding: 0 5px;
}

#workspaces button {
padding: 0 8px;
background-color: transparent;
color: #ffffff;
border: none;
border-radius: 0;
transition: all 0.3s ease;
}

#workspaces button:hover {
background: rgba(255, 0, 64, 0.2);
color: #ff0040;
text-shadow: 0 0 5px #ff0040;
}

#workspaces button.active {
background-color: #ff0040;
color: #000000;
font-weight: bold;
box-shadow: 0 0 8px #ff0040;
}

#workspaces button.focused {
background-color: #ff0040;
color: #000000;
}

#workspaces button.urgent {
background-color: #ff4444;
color: #ffffff;
animation: blink 0.5s linear infinite alternate;
}

@keyframes blink {
  to {
    background-color: #ffffff;
    color: #ff0040;
  }
}

#window {
background: #000000;
margin: 2px 5px;
padding: 2px 15px;
color: #ffffff;
}

#clock {
background: #000000;
margin: 2px 5px;
padding: 2px 15px;
color: #ffffff;
font-weight: bold;
}

/* This general block applies base styling to all modules listed, including custom/idle_inhibitor */
#battery,
#pulseaudio,
#tray,
#custom-idle_inhibitor, /* Corrected ID for your module */
#custom-audio,
#custom-bluetooth,
#custom-spotify, /* Added spotify and swaync here as they are also custom modules */
#custom-swaync {
background: #000000;
margin: 2px 5px;
padding: 2px 10px;
color: #ffffff;
}

#battery.charging,
#battery.plugged {
color: #ffffff;
}

#battery.critical:not(.charging) {
background-color: #ff4444;
color: #ffffff;
animation: blink 0.5s linear infinite alternate;
}

#pulseaudio.muted {
background: rgba(102, 102, 102, 0.2);
color: #666666;
}

#tray > .passive {
-gtk-icon-effect: dim;
}

#tray > .needs-attention {
-gtk-icon-effect: highlight;
background-color: #ff0040;
}

/* Specific styles for the "awake" and "idle" states of your custom/idle_inhibitor module */
#custom-idle_inhibitor.awake {
background-color: rgba(255, 0, 64, 0.3); /* Your neon red activated color */
color: #ff0040; /* Your neon red text color */
}

#custom-idle_inhibitor.idle {
background-color: #000000; /* Revert to default bar background for inactive state */
color: #ffffff; /* Default text color for inactive state */
}


#mode {
background: #000000;
border: 1px solid #ff0040;
border-radius: 0;
margin: 2px 5px;
padding: 2px 15px;
color: #ffffff;
}

tooltip {
  background: #000000;
  border: 1px solid #ff0040;
  border-radius: 0;
  color: #ffffff;
}

tooltip label {
  color: #ffffff;
}

#custom-volume {
padding: 0 10px;
color: #cdd6f4;
}

#custom-volume.muted {
color: #f38ba8;
}

/* Although not strictly needed for text, keeping this here for consistency with other modules */
#custom-idle_inhibitor {
font-family: "JetBrainsMono Nerd Font", "Symbols Nerd Font", "monospace";
}
